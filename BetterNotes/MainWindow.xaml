<Window x:Class="BetterNotes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BetterNotes"
        mc:Ignorable="d"
        Title="Better Notes" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Menu bar to appear at the top -->
            <RowDefinition Height="*" />
            <!-- Textbox will need to take most space -->
            <RowDefinition Height="Auto" />
            <!-- Status bar to appear at the bottom of the app -->
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Open File..." Click="OpenFile_Click"></MenuItem>
                <MenuItem Header="Save Note" Click="SaveFile_Click"></MenuItem>
                <MenuItem Header="Save As..." Click="SaveAs_Click"></MenuItem>
            </MenuItem>
        </Menu>
        
        <!-- Text Box -->
        <TextBox x:Name="NoteTextBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 FontSize="14" TextWrapping="NoWrap" 
                 Grid.Row="1" TextChanged="NoteTextBox_TextChanged"
                 SelectionChanged="NoteTextBox_SelectionChanged"/>

        
        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Name="CharCountText" Text="Characters: 0" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Name="LineColText" Text="Ln 1, Col 1" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Name="SaveStatusText" Text="Saved" />
            </StatusBarItem>
        </StatusBar>

    </Grid>
</Window>
